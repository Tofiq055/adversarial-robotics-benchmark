# Reproducibility Guide

This document explains how to reproduce the benchmark results reported in the paper.

## Docker Image Pinning

The simulation environment is built from two base images:

| Container | Base Image | Purpose |
|-----------|-----------|---------|
| `a4_sim` | `osrf/ros:humble-desktop` | ROS 2 Humble + Gazebo 11 + UR5e |
| `a4_testrunner` | `python:3.11-slim` | Orchestrator + static analyzer |
| `a4_ollama` | `ollama/ollama:latest` | Local LLM inference (GPU) |

For exact reproducibility, pin the Ollama image to a specific tag rather than `latest`. The image used in the published results is recorded in each run's `ENVIRONMENT.md` file.

## Seed and Parameter Locking

All models in the ablation study share identical inference parameters:

```
temperature = 0.7
seed        = 3407
repeat_penalty = 1.2
num_predict = 4096
```

These are enforced by `run_full_ablation.sh` and recorded in each run's `run_config.json`.

The seed `3407` was chosen following the recommendation from "Torch.manual_seed(3407) is all you need" (Picard, 2021) for reproducible ML experiments.

## Python Dependencies

All Python package versions are pinned in `requirements.txt`. The testrunner container installs these at build time. To verify locally:

```bash
pip install -r requirements.txt
```

## Model Weights

The fine-tuned model weights used in the ablation study are **not included** in this repository. See [ETHICS.md](../ETHICS.md) for the rationale.

To reproduce with your own models:

1. Fine-tune using the Modelfile template (`Modelfile_std`) as a starting point
2. Import into Ollama: `ollama create <name> -f Modelfile_std`
3. Run the benchmark: `make smoke` or `bash scripts/run_full_ablation.sh`

For access to the original weights used in the paper, contact the maintainers with proof of institutional affiliation.

## Per-Run Provenance

Each benchmark run automatically captures its full environment in three metadata files:

- `ENVIRONMENT.md` — OS, GPU, Docker versions, container status, disk space
- `MODEL_METADATA.md` — Per-model blob SHA256, Modelfile contents, training parameters
- `ACADEMIC_HYPOTHESES.md` — What the run is designed to test

These files are generated by `scripts/academic_metadata.py` before any model evaluation begins, ensuring the context is preserved even if the run crashes midway.

## Step-by-Step Reproduction

```bash
# 1. Clone and configure
git clone https://github.com/tofiq055/adversarial-robotics-benchmark.git
cd adversarial-robotics-benchmark
cp .env.example .env

# 2. Build and start containers
make up

# 3. Import your models into Ollama
# (see scripts/setup_ablation_models.sh for the model creation workflow)

# 4. Run pre-flight checks
make test

# 5. Run the full ablation
bash scripts/run_full_ablation.sh

# 6. Results are in data/results/runs/RUN_<timestamp>/
```

## See Also

- [STATISTICAL_POWER.md](STATISTICAL_POWER.md) — n=65 power analysis and observed Fisher's-exact effects
- [RUBRIC.md](RUBRIC.md) — Three-metric scoring rubric (static intent, sandbox UNSAFE, peak velocity)
- [DATASET_CARD.md](DATASET_CARD.md) — Provenance of the V2 -> V5 dataset family
- [THREAT_MODEL.md](THREAT_MODEL.md) — Attack surface and threat taxonomy (EN)
- [THREAT_MODEL_TR.md](THREAT_MODEL_TR.md) — Threat model (TR)
